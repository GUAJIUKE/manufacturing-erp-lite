[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short --strict-markers
markers =
    integration: end-to-end tests against the real MySQL test database
filterwarnings =
    ignore::DeprecationWarning
